{
	"name": "Existing Dockerfile",
	"build": {
		"context": "..",
		"dockerfile": "../Dockerfile"
	},
	"postCreateCommand": "cd '${containerWorkspaceFolder}' && ./build_thirdparty.sh",
	"postStartCommand": "sudo ldconfig",
	"customizations": {
		"vscode": {
			"extensions":["ms-vscode.cpptools-extension-pack"]
		}
	},
	"containerEnv": {
		"DISPLAY": ":0",
		"WAYLAND_DISPLAY": "${localEnv:WAYLAND_DISPLAY}",
		"XDG_RUNTIME_DIR": "${localEnv:XDG_RUNTIME_DIR}",
		"PULSE_SERVER": "${localEnv:PULSE_SERVER}"
	},
    "mounts": [
		"type=bind,src=\\\\wsl.localhost\\docker-desktop\\tmp\\.X11-unix,dst=/tmp/.X11-unix",
		"type=bind,src=\\\\wsl.localhost\\docker-desktop\\usr\\lib\\wsl,dst=/usr/lib/wsl"
	],
	"runArgs": ["--privileged"]
}
